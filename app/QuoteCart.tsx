'use client';
import{useEffect,useState}from'react';import{QUOTE_CART_KEY,QuoteItem,whatsappUrl}from'./cart';
export default function QuoteCart(){const[items,setItems]=useState<QuoteItem[]>([]);useEffect(()=>{const sync=()=>{try{setItems(JSON.parse(localStorage.getItem(QUOTE_CART_KEY)||'[]'))}catch{setItems([])}};sync();window.addEventListener('paper-press-cart',sync);window.addEventListener('storage',sync);return()=>{window.removeEventListener('paper-press-cart',sync);window.removeEventListener('storage',sync)}},[]);function open(){if(!items.length){window.location.href='/custom-size';return}window.open(whatsappUrl(items),'_blank','noopener,noreferrer')}return <button className="header-cart" onClick={open} aria-label={`WhatsApp quote cart with ${items.length} items`}><span>WhatsApp cart</span><b>{items.length}</b></button>}
