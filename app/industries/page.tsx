import {pageSeo} from '../../lib/seo';
import type {Metadata} from 'next';import Link from 'next/link';import {getCategories} from '../../lib/catalog';
export const metadata:Metadata=pageSeo('B2B Packaging by Industry — Paper & Press','Wholesale packaging systems for bakeries, cafés, QSR, ecommerce, garments, FMCG, healthcare and beauty brands.','/industries');
const familyIds=['bakery','food','cafe','courier','fashion','grocery','healthcare','beauty'];
export default async function IndustriesPage(){const b2bCategories=await getCategories();const catalogFamilies=[...new Set(b2bCategories.map(item=>item.family))];return <main><section className="industries-hero"><div><span>B2B solutions by industry</span><h1>Built around<br/>how you sell.</h1><p>Choose an industry to see relevant structures, practical quality tiers and indicative wholesale rates.</p><Link href="/quote">Discuss a supply program →</Link></div><img src="/catalog/masala-boxes-03.webp" alt="Wholesale branded cartons"/></section><nav className="industry-tabs">{catalogFamilies.map((family,index)=><a href={`#${familyIds[index]||`family-${index}`}`} key={family}>{family}</a>)}</nav>{catalogFamilies.map((family,index)=>{const items=b2bCategories.filter(i=>i.family===family);return <section id={familyIds[index]||`family-${index}`} className="industry-family" key={family}><div className="family-copy"><span>0{index+1}</span><h2>{family}</h2><p>{items.map(i=>i.name).join(', ')} and coordinated bulk supply for your complete requirement.</p><Link href={`/products?family=${encodeURIComponent(family)}`}>View this catalog →</Link></div><div className="family-products">{items.map(item=><Link href={`/categories/${item.slug}`} key={item.slug}><img src={item.images[0]} alt={item.name}/><div><h3>{item.name}</h3><span>{item.price}</span><small>{item.moq}</small></div></Link>)}</div></section>})}<section className="catalog-cta"><p>Multiple categories in one requirement?</p><h2>Send one consolidated brief.<br/>We’ll plan the supply.</h2><Link href="/quote">Request B2B pricing →</Link></section></main>}
