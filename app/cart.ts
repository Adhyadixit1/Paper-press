export const QUOTE_CART_KEY='paper-and-press-whatsapp-quote';
export type QuoteItem={id:string;category:string;sizeLabel:string;dimensions:string;volume:string;quantity:number;grade:string;branding:string;pin:string;estimatedRange:string;custom:boolean;notes?:string};
export function whatsappMessage(items:QuoteItem[]){return ['Hello Paper & Press Jaipur,','',`I would like pricing for ${items.length} wholesale packaging item${items.length===1?'':'s'}:`,...items.flatMap((item,index)=>['',`${index+1}. ${item.category}`,`Size: ${item.sizeLabel} (${item.dimensions})`,`Volume: ${item.volume}`,`Quantity: ${item.quantity.toLocaleString('en-IN')} pcs`,`Quality: ${item.grade}`,`Branding: ${item.branding}`,`Delivery PIN: ${item.pin||'To be confirmed'}`,`Estimated website range: ${item.estimatedRange}`,`Custom-size quote required: ${item.custom?'Yes':'No'}`,item.notes?`Notes: ${item.notes}`:''].filter(Boolean)),'','Please confirm material, GST, freight, production time and final price.'].join('\n')}
export function whatsappUrl(items:QuoteItem[]){return `https://wa.me/918824622541?text=${encodeURIComponent(whatsappMessage(items))}`}
